# Copy this file to .env.local and update values for local development
# If you're using Supabase CLI local stack, the defaults are typically:
# VITE_SUPABASE_URL=http://localhost:54321
# VITE_SUPABASE_ANON_KEY=<your-local-anon-key>

# Example (uncomment and replace the anon key):
# VITE_SUPABASE_URL=http://localhost:54321
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example-local-key

# If you don't set these variables the app will use the hosted Supabase values
# that are bundled as fallbacks in the code (not recommended for local dev).
